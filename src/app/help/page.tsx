import Link from "next/link";

const articles = [{ title: "Invite your team", text: "Open Settings, choose Team members, and invite teammates with the employee role." }, { title: "Connect Supabase", text: "Add the public Supabase URL and publishable key to your Vercel project, then redeploy." }, { title: "Confirmation email not arriving", text: "Check Supabase Auth SMTP settings, redirect URLs, and email delivery logs. Custom SMTP is recommended for production." }, { title: "Import leads", text: "Use the Import action on the pipeline and provide CSV columns for name, company, email, value, stage, and source." }];
export default function HelpPage() { return <main className="public-page"><div className="public-nav"><Link href="/" className="public-brand">orbit<span>.</span></Link><Link href="/">Back to app</Link></div><article className="public-copy help-copy"><p className="eyebrow">Support center</p><h1>How can we help?</h1><p className="lead-copy">Practical answers for setting up your agency workspace and keeping your pipeline moving.</p><div className="help-list">{articles.map((article) => <section key={article.title}><h2>{article.title}</h2><p>{article.text}</p></section>)}</div><p className="support-note">Still stuck? Contact your workspace administrator or review the deployment notes in the project README.</p></article></main>;
}
